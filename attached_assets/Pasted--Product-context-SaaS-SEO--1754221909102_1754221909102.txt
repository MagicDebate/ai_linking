# Product context
Это SaaS-сервис для SEO-специалистов, автоматизирующий внутреннюю перелинковку.  
Пользователь уже залогинился: нужно показать первый экран ЛК («Мои проекты»).

# Экран «Мои проекты»  — UI-требования
A. Header (sticky, 64 px)
   • Logo → кликом ведёт на /dashboard  
   • Profile dropdown ─► пункты: «Настройки», «Сменить пароль» (модалка внутри настроек), «Выход»

B. H1 + subtitle  
   H1 = «Ваши проекты»  
   Subtitle = «Управляйте внутренними ссылками в один клик»

C. Content area  
   1. Tile «Создать проект» (CTA)  
      • Карточка с пошаговой подписью: «1 — Укажите домен, 2 — Загрузите тексты, 3 — Получите черновик»  
   2. Если у пользователя есть проекты — карточки-строки:  
      • domain (крупно), project name (мелко)  
      • badge статуса Queued / Ready  
      • updatedAt («обновлено 3 дн. назад»)  
      • trailing menu ⋮ («Настройки», «Удалить»)  
      • при >5 проектов показывать кнопку «Показать все»

D. Right sidebar (340 px) — Onboarding checklist  
   • Title = «Первый запуск»                   ▣ progress-bar  
   • Шаг 1 «Создать проект»  
   • Шаг 2 «Загрузить тексты»  
   • Шаг 3 «Выбрать приоритеты»  
   • Шаг 4 «Сгенерировать черновик»  
   Галочка и заливка прогресса, когда шаг выполнен.  
   Если проектов нет — активен только 1-й шаг.

E. Notification banner (optional, dismiss-once)  
   • «Новый алгоритм детекта каннибализации доступен 💡 Подробнее»  
   • Хранить dismiss в localStorage.

F. FAQ accordion внизу страницы (свёрнут по умолчанию, max-width 800 px, центр)  
   1. «Сервис добавит спам-ссылки и меня забанит Google?» – ответ: алгоритм соответствует рекомендациям.  
   2. «Как убедиться, что ничего не публикуется без моего одобрения?» – ответ: генерация = черновик.  
   3. «Данные клиента под NDA, безопасно ли?» – ответ о шифровании и авто-удалении.  
   4. «Что с каноникой и каннибализацией?» – ответ о встроённом детекторе.  
   5. «Как защитить ручную сетку ссылок?» – ответ про стоп-лист.  
   6. «Поддерживает ли сервис нестандартные CMS?» – ответ про CSV/Git/WordPress-плагин.

# API stubs (можешь мокнуть JSON или in-memory)
GET /api/projects           → [{ id, name, domain, status, updatedAt }]
GET /api/progress           → { createProject, uploadTexts, setPriorities, generateDraft } // bools
GET /api/notifications      → [{ id, message, link, type }]
POST /api/notifications/:id/dismiss
